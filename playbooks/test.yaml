---
- name: Test
  hosts: localhost
  become: false

  tasks:

    # - name: Set test fact
    #   ansible.builtin.set_fact:
    #     test_bool: true

    - name: Debug
      when:
        - not test_bool | default(false) | bool
      ansible.builtin.debug:
        msg: "Test"